#include <iostream>
#include <bits/stdc++.h>
using namespace std;

// 22
int solve(vector<int> &prices, int len, int i)
{
    if (i == len)
    {
        return
    }
}

int rodCutting(vector<int> &prices, int len)
{
    int maxPrice = 0;
    sort(prices.begin(), prices.end(), greater<int>());
    return solve(prices, len, 0);
}

int main()
{

    return 0;
}