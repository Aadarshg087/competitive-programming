#include <iostream>
#include <bits/stdc++.h>
using namespace std;

vector<string> restoreIpAddresses(string s)
{
    // if s.size() >= 13 return empty vector
    //  if s.size() <=3 return empty vector
    //
}

int main()
{
    string s;
    cin >> s;
    vector<string> s = restoreIpAddresses(s);

    return 0;
}