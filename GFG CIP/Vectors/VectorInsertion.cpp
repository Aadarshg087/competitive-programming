#include <iostream>
#include <bits/stdc++.h>
using namespace std;

vector<int> fillVector(int arr[], int n)
{
    vector<int> v;
    for (int i = 0; i < n; i++)
    {
        v.push_back(arr[i]);
    }
    return v;
}

int main()
{

    return 0;
}