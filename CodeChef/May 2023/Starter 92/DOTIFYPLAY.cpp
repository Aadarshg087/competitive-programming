#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int total_songs, needed_songs, language;
        cin >> total_songs >> needed_songs >> language;
        int n = 2 * total_songs;
        int arr[n];
        for (int i = 0; i < n; i++)
        {
            cin >> arr[i];
        }

        for (int i = 0; i < n; i++)
        {
            
        }
                
    }

    return 0;
}