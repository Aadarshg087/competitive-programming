#include <iostream>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t-- > 0)
    {
        // Alice --> likes all the numebr which are divisible by A
        // Bob --> does not like all the which are divisible by B
        int a, b, n;
        cin >> a >> b >> n;
    }
    return 0;
}