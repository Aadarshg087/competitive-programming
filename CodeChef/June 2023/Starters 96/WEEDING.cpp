#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int number_of_weeds, number_of_days, sprays_to_kill;
        cin >> number_of_weeds >> number_of_days >> sprays_to_kill;
        long long int arr[number_of_weeds];
        for (int i = 0; i < number_of_weeds; i++)
        {
            cin >> arr[i];
        }
        if (sprays_to_kill >)
    }

    return 0;
}